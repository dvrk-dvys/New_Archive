tests:

  # This is just testing that the wrappers do compile an run, proper testing is still done in the wrapped suites.

  - name: classified-ad-interactions-suite.jslt compiles and runs
    type: run
    expression: |
      import "checks/p10n/classified-ad-interactions-suite.jslt" as c
      c(.)

  - name: click-recommender-event-suite.jslt compiles and runs
    type: run
    expression: |
      import "checks/p10n/click-recommender-event-suite.jslt" as c
      c(.)

  - name: content-events-for-cars-suite.jslt compiles and runs
    type: run
    expression: |
      import "checks/p10n/content-events-for-cars-suite.jslt" as c
      c(.)

  - name: content-events-suite.jslt compiles and runs
    type: run
    expression: |
      import "checks/p10n/content-events-suite.jslt" as c
      c(.)

  - name: impression-view-recommender-event-suite.jslt compiles and runs
    type: run
    expression: |
      import "checks/p10n/impression-view-recommender-event-suite.jslt" as c
      c(.)

  - name: leads-event-suite.jslt compiles and runs
    type: run
    expression: |
      import "checks/p10n/leads-event-suite.jslt" as c
      c(.)