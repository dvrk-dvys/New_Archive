tests:

  # This is just testing that the wrappers do compile an run, proper testing is still done in the wrapped suites.

  - name: experiment-assignment-events-suite.jslt compiles and runs
    type: run
    expression: |
      import "checks/houston/experiment-assignment-events-suite.jslt" as c
      c(.)
