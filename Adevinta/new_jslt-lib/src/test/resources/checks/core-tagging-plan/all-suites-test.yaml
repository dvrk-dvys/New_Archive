tests:

  # This is just testing that the wrappers do compile an run, proper testing is still done in the wrapped suites.

  - name: classified-ad-view-suite.jslt compiles and runs
    type: run
    expression: |
      import "checks/core-tagging-plan/classified-ad-view-suite.jslt" as c
      c(.)

  - name: form-viewed-suite.jslt compiles
    type: run
    expression: |
      import "checks/core-tagging-plan/form-viewed-suite.jslt" as c
      c(.)

  - name: listing-viewed-suite.jslt compiles
    type: run
    expression: |
      import "checks/core-tagging-plan/listing-viewed-suite.jslt" as c
      c(.)