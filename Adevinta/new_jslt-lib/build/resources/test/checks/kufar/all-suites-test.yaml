tests:

  # This is just testing that the wrappers do compile an run, proper testing is still done in the wrapped suites.

  - name: pump-accounts-suite.jslt compiles and runs
    type: run
    expression: |
      import "checks/kufar/pump-accounts-suite.jslt" as c
      c(.)

  - name: pump-ads-suite.jslt compiles and runs
    type: run
    expression: |
      import "checks/kufar/pump-ads-suite.jslt" as c
      c(.)
